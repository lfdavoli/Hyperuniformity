#include <fstream>
#include "functions.cpp"
using namespace std;

int main()
{
    srand(time(NULL));
    vector<vector<double>> lattice = create_lattice(10);
    double radii[200];
    for (size_t i = 0; i < 200; i++)
    {
        /* code */
    }
    
    
}